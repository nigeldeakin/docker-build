box: golang
services:
- name: postgres
  id: postgres
  env:
    POSTGRES_PASSWORD: test
    POSTGRES_USER: test
    POSTGRES_DB: test

build:
  steps:
    - wercker/golint
    - script:
       name: go build
       code: |
         ls -ltr
         go get -u github.com/lib/pq
         go build ./...
         ./docker-networks
